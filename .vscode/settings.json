{
    "debug.openDebug": "neverOpen",
    "saveAndRun": {
        "commands": [
          {
            "match": ".st",
            "cmd": "clear && gst ${fileBasename}"
          }
        ]
      }
}