project Projekt is

   for Object_Dir use "obj";
   for Main use ("Simulation.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-g");
   end Compiler;

   package Builder is
   for Executable ("Simulation.adb") use "projekt";
   end Builder;

end Projekt;
